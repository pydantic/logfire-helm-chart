{{- if and .Values.gateway.enabled .Values.gateway.create -}}
{{- $result := include "logfire.effective_hostnames" . | fromJson -}}
{{- $hosts := $result.hosts -}}
{{- $hasHosts := gt (len $hosts) 0 -}}
{{- $tls := include "logfire.effective_tls" . | eq "true" -}}
{{- $tlsSecret := include "logfire.effective_tls_secret" . -}}
{{- $gatewayName := .Values.gateway.name | default "logfire-gateway" -}}
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ $gatewayName }}
  labels:
    {{- include "logfire.labels" . | nindent 4 }}
    {{- with .Values.gateway.gatewayLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.gateway.gatewayAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  gatewayClassName: {{ required "gateway.gatewayClassName is required when gateway.create is true" .Values.gateway.gatewayClassName }}
  {{- if .Values.gateway.listeners }}
  listeners:
    {{- range $listener := .Values.gateway.listeners }}
    {{- if $hasHosts }}
    {{- range $idx, $host := $hosts }}
    - name: {{ $listener.name }}{{ if gt $idx 0 }}-{{ $idx }}{{ end }}
      protocol: {{ $listener.protocol }}
      port: {{ $listener.port }}
      hostname: {{ $host | quote }}
      {{- with $listener.tls }}
      tls:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $listener.allowedRoutes }}
      allowedRoutes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- end }}
    {{- else }}
    - name: {{ $listener.name }}
      protocol: {{ $listener.protocol }}
      port: {{ $listener.port }}
      {{- with $listener.tls }}
      tls:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $listener.allowedRoutes }}
      allowedRoutes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- end }}
    {{- end }}
  {{- else }}
  listeners:
    {{- if $tls }}
    {{- if $hasHosts }}
    {{- range $idx, $host := $hosts }}
    - name: https{{ if gt $idx 0 }}-{{ $idx }}{{ end }}
      protocol: HTTPS
      port: 443
      hostname: {{ $host | quote }}
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ $tlsSecret }}
      allowedRoutes:
        namespaces:
          from: Same
    {{- end }}
    {{- else }}
    - name: https
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ $tlsSecret }}
      allowedRoutes:
        namespaces:
          from: Same
    {{- end }}
    {{- else }}
    {{- if $hasHosts }}
    {{- range $idx, $host := $hosts }}
    - name: http{{ if gt $idx 0 }}-{{ $idx }}{{ end }}
      protocol: HTTP
      port: 80
      hostname: {{ $host | quote }}
      allowedRoutes:
        namespaces:
          from: Same
    {{- end }}
    {{- else }}
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
    {{- end }}
    {{- end }}
  {{- end }}
  {{- with .Values.gateway.addresses }}
  addresses:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
